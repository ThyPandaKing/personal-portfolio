# Pin the PUBLIC npm registry. Without this, npm may inherit a developer's
# personal ~/.npmrc pointing at a private registry that CI/deploy hosts can't
# reach, making `npm ci` hang then crash ("Exit handler never called!").
# All dependencies here are public packages.
registry=https://registry.npmjs.org/
